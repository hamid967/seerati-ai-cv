# Future Architecture

The target architecture is an explicit `AnonymousResumeSession` boundary around browser-memory resume state, import buffers, deletion, and privacy disclosure. Authenticated persistence remains an opt-in account capability. AI requests should receive only the selected task context, use structured Zod outputs, enforce request limits, and redact diagnostics. PDF output should retain a browser print fallback and add deterministic regression fixtures.
