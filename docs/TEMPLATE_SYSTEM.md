# Template System

Templates are component-driven and rendered from typed resume data. Each template must declare language direction support, A4 behavior, ATS suitability, print rules, and page-break expectations. The existing gallery contains more than the minimum requested template count; future additions must remain original and preserve selectable text, RTL/LTR support, and no-watermark export.
